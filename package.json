{
  "name": "realtime-chat-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Production-quality Slack/Discord-lite real-time chat application",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "dev:server": "npm run dev --workspace=@realtime-chat/server",
    "dev:web": "npm run dev --workspace=@realtime-chat/web",
    "lint": "npm run lint --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "test:coverage": "npm run test:coverage --workspace=@realtime-chat/server",
    "test:e2e": "npm run test:e2e --workspace=@realtime-chat/web",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "seed": "npm run seed --workspace=@realtime-chat/server",
    "docker:up": "docker compose up -d",
    "docker:down": "docker compose down"
  },
  "devDependencies": {
    "@types/node": "^20.11.24",
    "@typescript-eslint/eslint-plugin": "^7.18.0",
    "@typescript-eslint/parser": "^7.18.0",
    "eslint": "^8.57.1",
    "prettier": "^3.2.5",
    "typescript": "^5.4.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
